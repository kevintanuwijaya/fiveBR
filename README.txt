To Run web, please do this following steps:
1. run comment 'composer install' in terminal
2. configure the copy .env.example with the same directory and change the name into .env
3. configure the database name,username,password
4. run the migration and seeder with 'php artisan migrate:fresh --seed' on terminal